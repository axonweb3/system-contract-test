// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract BigContract {
    uint256 public resultA;
    uint256 public resultB;
    uint256 public resultC;
    uint256 public resultD;
    uint256 public resultE;
    uint256 public resultF;
    uint256 public resultG;
    uint256 public resultH;
    uint256 public resultI;
    uint256 public resultJ;

    uint256 public resultK;
    uint256 public resultL;
    uint256 public resultM;
    uint256 public resultN;
    uint256 public resultO;
    uint256 public resultP;
    uint256 public resultQ;
    uint256 public resultR;
    uint256 public resultS;
    uint256 public resultT;

    uint256 public resultU;
    uint256 public resultV;
    uint256 public resultW;
    uint256 public resultX;
    uint256 public resultY;
    uint256 public resultZ;

    uint256 public resultA1;
    uint256 public resultB1;
    uint256 public resultC1;
    uint256 public resultD1;
    uint256 public resultE1;
    uint256 public resultF1;
    uint256 public resultG1;
    uint256 public resultH1;
    uint256 public resultI1;
    uint256 public resultJ1;

    uint256 public resultK1;
    uint256 public resultL1;
    uint256 public resultM1;
    uint256 public resultN1;
    uint256 public resultO1;
    uint256 public resultP1;
    uint256 public resultQ1;
    uint256 public resultR1;
    uint256 public resultS1;
    uint256 public resultT1;

    uint256 public result1;
    uint256 public result2;
    uint256 public result3;
    uint256 public result4;
    uint256 public calculatedValue;



    function heavyComputationA() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultA = superFinalVal;
    }

    function heavyComputationB() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }

        resultB = superFinalVal;
    }

    function heavyComputationC() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultC = superFinalVal;
    }

    function heavyComputationD() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultD = superFinalVal;
    }

    function heavyComputationE() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultE = superFinalVal;
    }

    function heavyComputationF() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultF = superFinalVal;
    }

    function heavyComputationG() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultG = superFinalVal;
    }

    function heavyComputationH() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultH = superFinalVal;
    }

    function heavyComputationI() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultI = superFinalVal;
    }

    function heavyComputationJ() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultJ = superFinalVal;
    }




    function heavyComputationK() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultK = superFinalVal;
    }

    function heavyComputationM() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultM = superFinalVal;
    }

    function heavyComputationN() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }

        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }

        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }

        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }

        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }

        resultN = superFinalVal;
    }

    function heavyComputationO() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultO = superFinalVal;
    }

    function heavyComputationP() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultP = superFinalVal;
    }

    function heavyComputationQ() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultQ = superFinalVal;
    }

    function heavyComputationR() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultR = superFinalVal;
    }

    function heavyComputationS() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultS = superFinalVal;
    }

    function heavyComputationT() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultT = superFinalVal;
    }




    function heavyComputationU() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }

        resultU = superFinalVal;
    }
    function heavyComputationV() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }

        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }

        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultV = superFinalVal;
    }

    function heavyComputationW() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultW = superFinalVal;
    }

    function heavyComputationY() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultY = superFinalVal;
    }

    function heavyComputationZ() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }
        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }
        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }
        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }
        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }
        resultZ = superFinalVal;
    }




    function heavyComputationA1() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }

        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }

        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }

        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }

        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }

        resultA1 = superFinalVal;
    }
    function heavyComputationB1() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }

        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }

        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }

        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }

        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }

        resultB1 = superFinalVal;
    }

    function heavyComputationC1() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }

        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }

        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }

        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }

        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }

        resultC1 = superFinalVal;
    }

    function heavyComputationD1() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }

        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }

        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }

        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }

        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }

        resultD1 = superFinalVal;
    }

    function heavyComputationE1() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }

        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }

        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }

        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }

        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }

        resultE1 = superFinalVal;
    }

    function heavyComputationF1() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }

        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }

        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }

        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }

        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }

        resultF1 = superFinalVal;
    }

    function heavyComputationG1() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }

        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }

        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }

        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }

        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }

        resultG1 = superFinalVal;
    }

    function heavyComputationH1() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }

        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }

        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }

        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }

        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }

        resultH1 = superFinalVal;
    }
    function heavyComputationI1() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }

        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }

        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }

        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }

        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }

        resultI1 = superFinalVal;
    }
    function heavyComputationJ1() public {
        uint256 complexVal = 0;
        for (uint256 x = 1; x < 300; x++) {
            for (uint256 y = 1; y < 200; y++) {
                if (x + y != 0) {
                    complexVal += x * y * 2 / (x + y);
                }
            }
        }

        uint256 anotherVal = 1;
        for (uint256 i = 0; i < 50; i++) {
            if (i % 2 == 0) {
                anotherVal *= 3;
            } else {
                anotherVal *= 2;
            }
        }

        uint256 finalVal = complexVal + anotherVal;
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                finalVal -= k;
            } else {
                finalVal += k;
            }
        }

        uint256 superFinalVal = 0;
        for (uint256 z = 0; z < 200; z++) {
            superFinalVal += finalVal * 2 - z * 3 + z;
        }

        for (uint256 v = 1; v < 100; v++) {
            superFinalVal /= 2;
            superFinalVal *= 3;
            superFinalVal -= 5;
            superFinalVal += v;
        }

        resultJ = superFinalVal;
    }




    function heavyComputation1() public {
        uint256 val = 0;
        for (uint256 i = 1; i < 200; i++) {
            for (uint256 j = 1; j < 150; j++) {
                if (i + j != 0) {
                    val += i * j / (i + j);
                }
            }
        }
        result1 = val;
    }

    function heavyComputation2() public {
        uint256 val = 0;
        for (uint256 i = 1; i < 100; i++) {
            for (uint256 j = 1; j < 100; j++) {
                if (j % 3 == 0) {
                    val += i + j;
                } else {
                    val -= i * j;
                }
            }
        }
        result2 = val;
    }

    function heavyComputation3() public {
        uint256 val = 1;
        for (uint256 i = 1; i < 150; i++) {
            for (uint256 j = 1; j < 150; j++) {
                if (i % j == 0) {
                    val *= i;
                } else {
                    val /= j;
                }
            }
        }
        result3 = val;
    }

    function heavyComputation4() public {
        uint256 val = 0;
        for (uint256 i = 1; i < 200; i++) {
            for (uint256 j = 1; j < 150; j++) {
                if (i + j != 0) {
                    val += i * j * 2 / (i + j);
                }
            }
        }
        result4 = val;
    }

    function getResult() public view returns (uint256, uint256, uint256, uint256) {
        return (result1, result2, result3, result4);
    }

    function complexCalculation() public {
        uint256 value = 0;
        for (uint256 i = 1; i < 100; i++) {
            for (uint256 j = 1; j < 100; j++) {
                if (i % 2 == 0 && j % 3 == 0) {
                    value += i * j;
                } else if (i % 3 == 0 || j % 2 == 0) {
                    value -= i * j;
                } else {
                    value += i + j;
                }
            }
        }
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                value *= 2;
            } else {
                value /= 2;
            }
        }
        calculatedValue = value;
    }

    function complexCalculation1() public {
        uint256 value = 0;
        for (uint256 i = 1; i < 100; i++) {
            for (uint256 j = 1; j < 100; j++) {
                if (i % 2 == 0 && j % 3 == 0) {
                    value += i * j;
                } else if (i % 3 == 0 || j % 2 == 0) {
                    value -= i * j;
                } else {
                    value += i + j;
                }
            }
        }
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                value *= 2;
            } else {
                value /= 2;
            }
        }
        calculatedValue = value;
    }

    function complexCalculation2() public {
        uint256 value = 0;
        for (uint256 i = 1; i < 100; i++) {
            for (uint256 j = 1; j < 100; j++) {
                if (i % 2 == 0 && j % 3 == 0) {
                    value += i * j;
                } else if (i % 3 == 0 || j % 2 == 0) {
                    value -= i * j;
                } else {
                    value += i + j;
                }
            }
        }
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                value *= 2;
            } else {
                value /= 2;
            }
        }
        calculatedValue = value;
    }

    function complexCalculation3() public {
        uint256 value = 0;
        for (uint256 i = 1; i < 100; i++) {
            for (uint256 j = 1; j < 100; j++) {
                if (i % 2 == 0 && j % 3 == 0) {
                    value += i * j;
                } else if (i % 3 == 0 || j % 2 == 0) {
                    value -= i * j;
                } else {
                    value += i + j;
                }
            }
        }
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                value *= 2;
            } else {
                value /= 2;
            }
        }
        calculatedValue = value;
    }

    function complexCalculation4() public {
        uint256 value = 0;
        for (uint256 i = 1; i < 100; i++) {
            for (uint256 j = 1; j < 100; j++) {
                if (i % 2 == 0 && j % 3 == 0) {
                    value += i * j;
                } else if (i % 3 == 0 || j % 2 == 0) {
                    value -= i * j;
                } else {
                    value += i + j;
                }
            }
        }
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                value *= 2;
            } else {
                value /= 2;
            }
        }
        calculatedValue = value;
    }

    function complexCalculation5() public {
        uint256 value = 0;
        for (uint256 i = 1; i < 100; i++) {
            for (uint256 j = 1; j < 100; j++) {
                if (i % 2 == 0 && j % 3 == 0) {
                    value += i * j;
                } else if (i % 3 == 0 || j % 2 == 0) {
                    value -= i * j;
                } else {
                    value += i + j;
                }
            }
        }
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                value *= 2;
            } else {
                value /= 2;
            }
        }
        calculatedValue = value;
    }

    function complexCalculation6() public {
        uint256 value = 0;
        for (uint256 i = 1; i < 100; i++) {
            for (uint256 j = 1; j < 100; j++) {
                if (i % 2 == 0 && j % 3 == 0) {
                    value += i * j;
                } else if (i % 3 == 0 || j % 2 == 0) {
                    value -= i * j;
                } else {
                    value += i + j;
                }
            }
        }
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                value *= 2;
            } else {
                value /= 2;
            }
        }
        calculatedValue = value;
    }

    function complexCalculation7() public {
        uint256 value = 0;
        for (uint256 i = 1; i < 100; i++) {
            for (uint256 j = 1; j < 100; j++) {
                if (i % 2 == 0 && j % 3 == 0) {
                    value += i * j;
                } else if (i % 3 == 0 || j % 2 == 0) {
                    value -= i * j;
                } else {
                    value += i + j;
                }
            }
        }
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                value *= 2;
            } else {
                value /= 2;
            }
        }
        calculatedValue = value;
    }

    function complexCalculation8() public {
        uint256 value = 0;
        for (uint256 i = 1; i < 100; i++) {
            for (uint256 j = 1; j < 100; j++) {
                if (i % 2 == 0 && j % 3 == 0) {
                    value += i * j;
                } else if (i % 3 == 0 || j % 2 == 0) {
                    value -= i * j;
                } else {
                    value += i + j;
                }
            }
        }
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                value *= 2;
            } else {
                value /= 2;
            }
        }
        calculatedValue = value;
    }

    function complexCalculation9() public {
        uint256 value = 0;
        for (uint256 i = 1; i < 100; i++) {
            for (uint256 j = 1; j < 100; j++) {
                if (i % 2 == 0 && j % 3 == 0) {
                    value += i * j;
                } else if (i % 3 == 0 || j % 2 == 0) {
                    value -= i * j;
                } else {
                    value += i + j;
                }
            }
        }
        for (uint256 k = 1; k < 100; k++) {
            if (k % 2 == 0) {
                value *= 2;
            } else {
                value /= 2;
            }
        }
        calculatedValue = value;
    }

    function getCalculatedValue() public view returns (uint256) {
        return calculatedValue;
    }
}
